post_install() {
    systemd-tmpfiles --create /usr/lib/tmpfiles.d/ezkvm.conf || true

    if command -v apparmor_parser >/dev/null 2>&1 && [ -f /etc/apparmor.d/usr.bin.swtpm ]; then
        apparmor_parser -r /etc/apparmor.d/usr.bin.swtpm || true
    fi
}

post_upgrade() {
    post_install
}
