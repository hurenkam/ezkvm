---
phase: 07-qemu-cmdline
plan: 05
type: execute
wave: 5
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - tests/qemu_cmdline.rs
autonomous: true
requirements: [QEMU-02, QEMU-03, QEMU-01]

must_haves:
  truths:
    - "For the felucia-108 golden-fixture Runtime, every '-device ...,drive=drive-X' token's string position in the full Display output is strictly greater than its corresponding '-drive ...,id=drive-X' token's position, for every X the fixture produces (QEMU-02, D-04/D-05)"
    - "For a synthetic Runtime containing at least one VirtioNetPcie (felucia only has one NIC — RESEARCH.md Pitfall 7 says this alone is insufficient to prove netdev-ordering robustness), every '-device virtio-net-pci,...,netdev=net-N' token's position is strictly greater than its corresponding '-netdev ...,id=net-N' token's position (QEMU-02)"
    - "RawArgs's verbatim blob appears in the output exactly once, as a contiguous unbroken substring, positioned after every other segment's last token (QEMU-03) — re-verified end-to-end now that Plans 07-02/07-03/07-04 have added devices/netdevs/objects/tpm segments after Plan 07-01 built only machine/firmware/drives/chardevs/tpm"
  artifacts:
    - tests/qemu_cmdline.rs
  key_links:
    - "assert_precedes(haystack, needle_before, needle_after) helper (Wave 0 requirement per 07-VALIDATION.md) -> reused across every drive/device and netdev/device assertion in this file, not reimplemented per-test"
  prohibitions:
    - "Must NOT assert byte-for-byte equality against input/felucia/108.qemu.cmd (D-05 — structural/ordering assertions only)"
    - "Must NOT rely solely on the felucia fixture to prove netdev-ordering — RESEARCH.md Pitfall 7 explicitly requires a synthetic VirtioNetPcie test since ProxmoxImporter's net wiring (Plan 07-03) produces at most the fixture's single NIC, which is not by itself sufied evidence of general correctness"
---

<objective>

Write the phase's final Nyquist-closing validation: `tests/qemu_cmdline.rs`, an integration test
asserting the two structural guarantees `QEMU-02` requires (drive-before-device,
netdev-before-device) via string-position comparison — not substring presence — plus a regression
check that `RawArgs`'s verbatim placement (`QEMU-03`, proven in isolation by Plan 07-01) still holds
true once every other segment Plans 07-02/07-03/07-04 added is populated.

- **Purpose:** This is the ROADMAP's originally-scoped plan bullet 07-04 ("write ordering validation
  test") and the last of `D-04` / `D-05`'s testing-strategy decisions — it is the only place in the
  phase where the felucia golden fixture and synthetic per-device construction are combined into the
  single coverage+ordering proof the phase's 4 success criteria require.
- **Output:** `tests/qemu_cmdline.rs` created with an `assert_precedes` helper, a felucia-fixture
  ordering test, and a synthetic multi-device-family ordering test.

</objective>

<execution_context>

@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md

</execution_context>

<context>

@`.planning/PROJECT.md`
@`.planning/ROADMAP.md`
@`.planning/phases/07-qemu-cmdline/07-CONTEXT.md`
@`.planning/phases/07-qemu-cmdline/07-RESEARCH.md`
@`.planning/phases/07-qemu-cmdline/07-VALIDATION.md`
@`tests/proxmox_import.rs`

</context>

## Artifacts this phase produces

Symbols newly created by THIS plan (exclude from drift/regression checks against pre-existing code):

- `assert_precedes` (fn, `tests/qemu_cmdline.rs`) — reusable ordering-assertion helper per 07-VALIDATION.md's Wave 0 requirement
- `test_qemu_cmdline_felucia_108_drive_before_device_ordering` (test, `tests/qemu_cmdline.rs`)
- `test_qemu_cmdline_felucia_108_rawargs_verbatim_at_end` (test, `tests/qemu_cmdline.rs`)
- `test_qemu_cmdline_synthetic_netdev_before_device_ordering` (test, `tests/qemu_cmdline.rs`)

<tasks>

<task type="auto" tdd="true">

<name>

#### Task 1: assert_precedes helper + felucia golden-fixture ordering test

</name>

<files>

`tests/qemu_cmdline.rs`

</files>

<read_first>

`tests/proxmox_import.rs` (load_felucia_108 helper — reuse this pattern), `input/felucia/108.conf`,
`input/felucia/storage.cfg`, `src/config/qemu.rs` (`QemuContext`'s fields — vm_name, ovmf_code_path,
tpm_socket_path)

</read_first>

<behavior>

- **Test 1:** `assert_precedes(haystack, "id=drive-scsi0", "drive=drive-scsi0")` passes when the first substring's byte index is less than the second's, and panics with a message naming both needles and their positions when it is not (or when either is missing).
- **Test 2:** Loading `input/felucia/108.conf` + `input/felucia/storage.cfg` via `ProxmoxImporter::new(..., 108).into_runtime()`, then `QemuCommandLine::try_from((runtime, QemuContext::new("felucia".to_string(), "/usr/share/OVMF/OVMF_CODE.fd".to_string(), Some("/tmp/felucia-tpm.sock".to_string()))))`, rendered via `.to_string()`, satisfies:
  - `assert_precedes(&output, "id=drive-scsi0", "drive=drive-scsi0")`
  - `assert_precedes(&output, "id=drive-ide2", "drive=drive-ide2")`
  - these are the fixture's two known storage devices (scsi0 from the boot disk, ide2 from the cdrom)
- **Test 3:** The same felucia-derived output:
  - contains the fixture's `RawArgs` blob (read `self.vm_conf.args`'s raw string value from the parsed conf directly for comparison, or assert a known substring from `108.conf`'s `args:` line) as one contiguous substring
  - satisfies `output.rfind(&rawargs_substring).unwrap() > output.rfind("-device").unwrap()`
  - this proves `RawArgs` is the last thing emitted even with devices/netdevs/objects segments now populated by Plans 07-02/07-03/07-04

</behavior>

<action>

- Create `tests/qemu_cmdline.rs` importing:
  - `ezkvm::config::proxmox::{ProxmoxImporter, ProxmoxStorageConf, ProxmoxVmConf}`
  - `ezkvm::config::qemu::{QemuCommandLine, QemuContext}`
  - `std::str::FromStr`
- Implement:

  ```rust
  fn assert_precedes(haystack: &str, needle_before: &str, needle_after: &str) {
      let before_pos = haystack
          .find(needle_before)
          .unwrap_or_else(|| panic!("'{}' not found in output", needle_before));
      let after_pos = haystack
          .find(needle_after)
          .unwrap_or_else(|| panic!("'{}' not found in output", needle_after));
      assert!(
          before_pos < after_pos,
          "expected '{}' (pos {}) to precede '{}' (pos {})",
          needle_before,
          before_pos,
          needle_after,
          after_pos
      );
  }
  ```

- Reuse `tests/proxmox_import.rs`'s `load_felucia_108()` shape:
  - copy, do not import
  - integration test files in Rust cannot share code across binaries without a shared test-support module, which is out of scope for this plan
- Load the fixture.
- Build the `Runtime` via `ProxmoxImporter::new(vm_conf, storage_conf, 108).into_runtime().expect("into_runtime")`.
- Build a `QemuContext` with plausible fixed paths.
- Per `D-06`, this test constructs `QemuContext` itself; it is not parsed.
- Convert via `QemuCommandLine::try_from((runtime, ctx)).expect("try_from")`.
- Render via `.to_string()`.
- Run the three `assert_precedes` / `RawArgs` assertions from the behavior spec above.

</action>

<verify>

<automated>

cargo test --test qemu_cmdline

</automated>

</verify>

<done>

All three behavior tests pass against the real felucia fixture, using position-based assertions only
(no byte-for-byte comparison against `108.qemu.cmd`, per `D-05`).

</done>

<acceptance_criteria>

- `assert_precedes` panics with both needle strings and their positions in the message when order is violated
- felucia-derived output: `id=drive-scsi0` position < `drive=drive-scsi0` position
- felucia-derived output: `id=drive-ide2` position < `drive=drive-ide2` position
- felucia-derived output: `RawArgs` substring's last occurrence position > the last `-device` token's position

</acceptance_criteria>

</task>

<task type="auto" tdd="true">

<name>

#### Task 2: synthetic netdev-before-device ordering test (closes RESEARCH.md Pitfall 7)

</name>

<files>

`tests/qemu_cmdline.rs`

</files>

<read_first>

`src/runtime/devices/pcie_net.rs`, `src/runtime/q35.rs` (`Q35ChipsetBuilder::with_pcie_device`),
`tests/runtime_phase2.rs` (synthetic `Runtime` / `Q35Chipset` construction pattern)

</read_first>

<behavior>

- **Test 1:** A synthetic `Runtime` built via `RuntimeBuilder::new().with_memory(Memory::new(4096)).with_chipset(Chipset::Q35(Q35ChipsetBuilder::new().with_pcie_device(Some(PcieAddress::new(20,0)), Arc::new(VirtioNetPcie::new(Some("vmbr0".to_string()), Some("AA:BB:CC:DD:EE:FF".to_string()), Some(1024), Some(256), Some(true)))).build())).build().unwrap()`, converted through `QemuCommandLine::try_from`, satisfies `assert_precedes(&output, "id=net20", "netdev=net20")` — this is the exact assertion RESEARCH.md's Pitfall 7 says the felucia fixture alone cannot exercise (only one NIC in that fixture, and per Plan 07-03's importer wiring it lands at a fixed offset, not necessarily proving the general case with a hand-constructed address).
- **Test 2:** The same synthetic `Runtime`'s output does **not** contain `bootindex=` anywhere (empty `boot_order` — regression guard confirming Plan 07-04's Task 3 behavior holds from the test-file level too, not just `handlers/root.rs`'s own unit tests).

</behavior>

<action>

- Add `test_qemu_cmdline_synthetic_netdev_before_device_ordering` to `tests/qemu_cmdline.rs`.
- Construct the synthetic `Runtime` / `Q35Chipset` / `VirtioNetPcie` exactly as described in the behavior spec.
- Import at the top of the file alongside Task 1's imports:
  - `ezkvm::runtime::{Runtime, RuntimeBuilder, Memory, Chipset, Q35ChipsetBuilder, PcieAddress, VirtioNetPcie}`
  - `std::sync::Arc`
- Build a `QemuContext` with the same fixed-path pattern as Task 1's felucia test.
- Convert, render, and run both:
  - `assert_precedes`
  - the `bootindex=` absence assertion

</action>

<verify>

<automated>

cargo test --test qemu_cmdline

</automated>

</verify>

<done>

Both tests pass; the phase's `QEMU-02` netdev-ordering guarantee is now proven independently of the
felucia fixture's single-NIC limitation.

</done>

<acceptance_criteria>

- synthetic `Runtime`: `id=net20` position < `netdev=net20` position
- synthetic `Runtime`'s output contains zero `bootindex=` tokens (empty `boot_order` case)
- `cargo test --test qemu_cmdline` exits 0 with 3+ passing tests total across both tasks in this plan

</acceptance_criteria>

</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test fixture files (`input/felucia/`*) → test process | Local repo-committed fixture files read via `std::fs::read_to_string`; not a runtime trust boundary, matches the existing pattern in `tests/proxmox_import.rs` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Information Disclosure | Test-constructed `QemuContext` uses fixed placeholder paths (`/tmp/felucia-tpm.sock`, `/usr/share/OVMF/OVMF_CODE.fd`) | low | accept | These are test-only, never-executed string values used solely to satisfy `QemuContext`'s required fields for a `Display` render — no process is spawned, no filesystem access to these paths occurs |

No threat in this plan meets the `high` severity bar under ASVS level 1 — this is a pure test-file
addition with no new runtime code path. No blocking checkpoint required.

</threat_model>

<verification>

`cargo test --test qemu_cmdline` green; `cargo test` (full suite) green, confirming no regression to
`tests/proxmox_import.rs` or `tests/runtime_phase2.rs`.

</verification>

<success_criteria>

- Drive-before-device and netdev-before-device ordering are both proven via string-position assertions, not substring presence (`QEMU-02`).
- `RawArgs` verbatim-at-end placement is re-confirmed end-to-end after all of Plans 07-02/07-03/07-04's additions (`QEMU-03`).
- The netdev-ordering guarantee is proven independently of the felucia fixture's single-NIC limitation (RESEARCH.md Pitfall 7 closed).

</success_criteria>

<output>

Create `.planning/phases/07-qemu-cmdline/07-05-SUMMARY.md` when done

</output>
