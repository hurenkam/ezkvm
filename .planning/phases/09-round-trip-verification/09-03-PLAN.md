---
phase: 09-round-trip-verification
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tests/round_trip_verification.rs
autonomous: true
requirements: [QEMU-04]

must_haves:
  truths:
    - "input/coruscant/501.conf + storage.cfg imports, round-trips through ezkvm YAML, and generates a QEMU cmdline with zero errors or panics, including its 3 hostpci devices, 3 scsi disks on a shared virtio-scsi-pci controller, and multi-line ivshmem RawArgs blob"
    - "input/zbp-server-mh2/301.conf + storage.cfg imports, round-trips through ezkvm YAML, and generates a QEMU cmdline with zero errors or panics, including its 6 usb devices (4 bus-port + 2 vendor:product-ID, already supported by Phase 8.1's typed UsbHostIdentity) and 4 per-disk virtio-scsi-single controllers"
    - "Each corpus test asserts the scsihw-controller-specific device string actually emitted for that corpus file's declared scsihw value (virtio-scsi-pci shared controller for coruscant, per-disk virtio-scsi-pci-model virtio-scsi-single controllers for zbp-server-mh2), per Phase 8.1's landed GenericScsiController/VirtioScsiSingleDisk wiring — no longer generic scsi presence-only assertions"
  artifacts:
    - "tests/round_trip_verification.rs (extended with 2 new #[test] functions)"
  key_links:
    - "load_corpus('coruscant', '501.conf') / load_corpus('zbp-server-mh2', '301.conf') -> runtime_for_cmdline() -> QemuCommandLine::try_from"
    - "zbp-server-mh2/301's usb4/usb5 vendor:product-ID emission -> Phase 8.1's UsbHostIdentity::VendorProduct + emit_usb_device (already landed, no in-phase fix needed)"
    - "coruscant/501's scsihw: virtio-scsi-pci -> importer.rs's ScsiControllerType match -> GenericScsiController -> emit_scsi_controller's virtio-scsi-pci device string"
    - "zbp-server-mh2/301's scsihw: virtio-scsi-single -> importer.rs's per-scsiN VirtioScsiSingleDisk construction -> emit_virtio_scsi_single's per-instance virtio-scsi-pci controller device string"
---

<objective>
Extend `tests/round_trip_verification.rs` (created by Plan 09-01) with two more corpus round-trip
tests — `input/coruscant/501.conf` and `input/zbp-server-mh2/301.conf` — per `09-CONTEXT.md` D-03's
corpus selection, reusing Plan 09-01's `load_corpus()`/`runtime_for_cmdline()`/`make_ctx()` helpers
verbatim. This completes QEMU-04's full three-corpus-file coverage.

This plan was replanned 2026-07-29 against Phase 8.1's landed baseline (see
`09-REPLAN-NOTES.md`): it no longer depends on a dropped `09-02-PLAN.md` (Phase 8.1 already fixed
USB vendor:product-ID passthrough properly, at the schema level, via a typed `UsbHostIdentity`),
and it now asserts scsihw-controller-specific device strings for both corpus files instead of
avoiding them — Phase 8.1 also fully wired `scsihw` through the pipeline instead of discarding it.

Purpose: Prove the round-trip pipeline holds not just for felucia's straightforward shape but for
coruscant's multi-hostpci/multi-ivshmem-RawArgs/NUMA-flag/virtio-scsi-pci shape and
zbp-server-mh2's multi-USB-syntax/virtio-scsi-single shape.
Output: `tests/round_trip_verification.rs` gains 2 new passing `#[test]` functions (one per corpus),
bringing this file's total to 3 tests covering all three D-03 corpus targets.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- New test `coruscant_501_full_round_trip_produces_valid_qemu_cmdline` in
  `tests/round_trip_verification.rs`
- New test `zbp_server_mh2_301_full_round_trip_produces_valid_qemu_cmdline` in
  `tests/round_trip_verification.rs`
</artifacts_produced>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-round-trip-verification/09-CONTEXT.md
@.planning/phases/09-round-trip-verification/09-RESEARCH.md
@.planning/phases/09-round-trip-verification/09-REPLAN-NOTES.md
@.planning/phases/08.1-usb-scsi-schema-extension/08.1-VERIFICATION.md
@tests/round_trip_verification.rs
@src/config/qemu/handlers/pcie.rs
@src/config/qemu/handlers/usb.rs
@input/coruscant/501.conf
@input/coruscant/storage.cfg
@input/coruscant/501.lspci.txt
@input/zbp-server-mh2/301.conf
@input/zbp-server-mh2/storage.cfg
@input/zbp-server-mh2/301.lsusb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: coruscant/501 four-stage round-trip integration test</name>
  <files>tests/round_trip_verification.rs</files>
  <precondition>Plan 09-01's tests/round_trip_verification.rs exists with load_corpus()/runtime_for_cmdline()/make_ctx() helpers already defined</precondition>
  <read_first>
    tests/round_trip_verification.rs (Plan 09-01's helpers — `load_corpus`, `runtime_for_cmdline`,
    `make_ctx` — and its felucia test's exact assertion style, including its `pvscsi`-specific
    controller assertion, to mirror here for `virtio-scsi-pci`);
    input/coruscant/501.conf (active section only, top of file before `[after_upgrade_to_plucky_puffin]`
    — device keys: agent, args (3x ivshmem device/object pairs referencing ivshmem12/43/44),
    balloon (ignored), bios, boot=`order=scsi0;ide2` — no net0 in this corpus's active section,
    cores, cpu, efidisk0, hostpci0 (`0000:07:00,pcie=1,rombar=0`, 1 function, no x-vga), hostpci1
    (`0000:41:00,pcie=1`, 1 function, no x-vga despite real hardware being dual-function per lspci —
    this is correct existing behavior per RESEARCH.md, not a gap), hostpci2 (`0000:0e:11.0,pcie=1`,
    1 function), hugepages (ignored), ide2, machine, memory, numa=1 (boolean parsed; numa0: sub-key
    NOT parsed — ignored), scsi0/scsi1/scsi2 (3 disks across vm1-pool/ws0/ws0-pool sharing ONE
    `virtio-scsi-pci` controller), scsihw=`virtio-scsi-pci` (now fully consumed by
    `ProxmoxImporter` per Phase 8.1 — produces a `GenericScsiController{controller_type:
    VirtioScsiPci}`, NOT a pvscsi default; see `08.1-VERIFICATION.md` Truth 6), serial0 (parsed,
    not consumed for cmdline), spice_enhancements/unused0-2 (ignored));
    src/config/qemu/handlers/pcie.rs (`emit_scsi_controller` — confirm the exact emitted string is
    `-device virtio-scsi-pci,id=scsihw0,bus=pci.0,addr=0x5` for `ScsiControllerType::VirtioScsiPci`,
    and that scsi1/scsi2 sub-disks use `drive-scsi{target}`/`scsi{target}` ids with
    `bus=scsihw0.0`, mirroring the pvscsi shared-bus shape Plan 09-01 already asserted for felucia);
    input/coruscant/501.lspci.txt (hostpci real-hardware cross-reference: Fresco Logic USB
    controller at hostpci0, AMD Radeon RX 570 at hostpci1, Intel X550 VF Ethernet at hostpci2 — used
    as documentation only, per D-04's the agent's Discretion, not parsed programmatically);
    .planning/phases/09-round-trip-verification/09-RESEARCH.md Pitfall 4 (RawArgs verbatim —
    coruscant's args blob contains 3 distinct ivshmem12/43/44 cross-references that must survive
    verbatim as ONE contiguous substring, not be reordered). NOTE: RESEARCH.md's Pitfall 3 (scsihw
    parsed-but-never-consumed) is STALE — Phase 8.1 fixed this; do not follow that pitfall's
    "assert generic presence only" guidance, see `09-REPLAN-NOTES.md`.
  </read_first>
  <behavior>
    - Test: `coruscant_501_full_round_trip_produces_valid_qemu_cmdline` — the full four-stage chain
      (Proxmox parse → Runtime → YAML → Runtime → QEMU cmdline) completes with zero panics/errors.
    - Test: cmdline contains exactly 3 `vfio-pci` device entries (hostpci0, hostpci1, hostpci2, each
      1 function since none has `x-vga=1` in this corpus's active section).
    - Test: cmdline contains exactly 1 occurrence of
      `-device virtio-scsi-pci,id=scsihw0,bus=pci.0,addr=0x5` (the shared controller line,
      controller-type-specific per coruscant's actual `scsihw: virtio-scsi-pci` value — Phase 8.1's
      `GenericScsiController`/`emit_scsi_controller` wiring), and 3 scsi `-drive`/`-device` pairs
      (`drive-scsi0`, `drive-scsi1`, `drive-scsi2`) attached to that controller (`bus=scsihw0.0`),
      each drive preceding its device (drive-before-device ordering, D-04).
    - Test: the active section's full `args:` blob (captured from `vm_conf.args` before import,
      containing `ivshmem12`, `ivshmem43`, `ivshmem44`) appears exactly once, verbatim, positioned
      after the last real emitted device token.
    - Test: cmdline contains NO `virtio-net-pci`/`netdev=net0` token (coruscant's active section has
      no `net0` key — confirms the importer does not spuriously fabricate a NIC).
  </behavior>
  <action>
    Add `#[test] fn coruscant_501_full_round_trip_produces_valid_qemu_cmdline()` to
    `tests/round_trip_verification.rs`, mirroring Plan 09-01's felucia test's structure exactly:
    `load_corpus("coruscant", "501.conf")` → capture `vm_conf.args.clone().expect(...)` →
    `ProxmoxImporter::new(vm_conf, storage_conf, 501).into_runtime().expect(...)` → YAML round-trip
    via `EzkvmConfigSchema::try_from`/`.to_styled_compact_yaml()`/`EzkvmConfigSchema::from_str`/
    `Runtime::try_from` → `runtime_for_cmdline(...)` → `QemuCommandLine::try_from((runtime,
    make_ctx("coruscant"))).expect(...)` → `output = cmdline.to_string()`. Assert:
    `output.matches("vfio-pci").count() == 3`;
    `output.contains("-device virtio-scsi-pci,id=scsihw0,bus=pci.0,addr=0x5")` (the
    controller-type-specific shared-controller line — this is the assertion Phase 8.1 made
    possible and this phase should now exercise, replacing the old generic-presence-only
    approach); 3 scsi drive/device pairs present via `output.matches("bus=scsihw0.0").count() == 3`
    (or exact per-id `id=drive-scsi{0,1,2}` presence checks) with each drive preceding its device
    (inline `.find()` position comparisons, same pattern as Plan 09-01); RawArgs verbatim-once-at-end
    using the exact `cmdline.devices().last()` + `output[..rawargs_pos].rfind(...)` pattern from
    Plan 09-01; `!output.contains("netdev=net0")` and `!output.contains("virtio-net-pci")` (no
    fabricated NIC).
  </action>
  <verify>
    <automated>cargo test --test round_trip_verification coruscant -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test round_trip_verification coruscant -- --nocapture` exits 0 with
      `coruscant_501_full_round_trip_produces_valid_qemu_cmdline` reported as passed.
    - `grep -c "id=scsihw0,bus=pci.0,addr=0x5" tests/round_trip_verification.rs` returns at least `1`
      (the controller-type-specific assertion is present, not omitted).
  </acceptance_criteria>
  <done>coruscant/501's full four-stage round trip is proven end-to-end, with structural assertions covering its 3 hostpci devices, its shared `virtio-scsi-pci` controller and 3 attached scsi disks (controller-type-specific, not generic), verbatim multi-ivshmem RawArgs, and absence of a fabricated NIC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: zbp-server-mh2/301 four-stage round-trip integration test (USB vendor:product-ID + virtio-scsi-single path)</name>
  <files>tests/round_trip_verification.rs</files>
  <precondition>Plan 09-01's tests/round_trip_verification.rs exists with load_corpus()/runtime_for_cmdline()/make_ctx() helpers already defined; Phase 8.1's typed UsbHostIdentity and VirtioScsiSingleDisk are already landed in src/ (verified — see 08.1-VERIFICATION.md), so no in-phase production-code fix is required before this test can pass</precondition>
  <read_first>
    tests/round_trip_verification.rs (Plan 09-01's helpers and Task 1's coruscant test, to mirror
    structure);
    src/config/qemu/handlers/usb.rs (Phase 8.1's landed `UsbHostIdentity::{BusPort,VendorProduct}`
    match arms in `emit_usb_device` — confirm the exact `vendorid=`/`productid=` flag names and
    hex formatting actually emitted before writing this test's assertions);
    src/config/qemu/handlers/pcie.rs (`emit_virtio_scsi_single` — confirm each `scsiN` disk gets
    its own `-device virtio-scsi-pci,id=scsihw{N},bus=pci.0,addr=0x...,iothread=iothread{N}`
    controller line, where `{N}` is the Proxmox `scsiN` index, and that `storage::emit_scsi_storage`
    produces `drive-scsi0`/`scsi0` for index 0 but `drive-scsi{N}-0`/`scsi{N}-0` for index N>0 — read
    the actual code, do not assume a uniform id format across all 4 instances);
    input/zbp-server-mh2/301.conf (active section only, top of file — device keys: agent, bios,
    boot=`order=scsi2`, cores/sockets/cpu, efidisk0, hostpci0 (`0000:6e:00.0,pcie=1`, 1 function),
    hugepages (ignored), ide2, machine, memory, net0 (`virtio=82:E2:0B:47:FE:68,bridge=vmbr0`),
    numa=1 (boolean only, no numaN: sub-keys present), scsi0-scsi3 (4 disks across vm0/ws0, each on
    its OWN `virtio-scsi-single` controller instance — no shared bus), scsihw=`virtio-scsi-single`
    (now fully consumed by `ProxmoxImporter` per Phase 8.1 — produces one `VirtioScsiSingleDisk` per
    `scsiN` index; see `08.1-VERIFICATION.md` Truth 7), serial0 (ignored for cmdline), unused1-3
    (ignored), usb0 (`host=1-4`, bus-port), usb1 (`host=1-5`, bus-port), usb2 (`host=1-7.6`,
    bus-port), usb3 (`host=1-7.5.1`, bus-port), usb4 (`host=0451:16a0`, vendor:product-ID —
    supported since Phase 8.1's typed `UsbHostIdentity`), usb5 (`host=0403:6001`,
    vendor:product-ID — same), vga (`virtio-gl,memory=64` — not modeled as a distinct device beyond
    existing scope));
    input/zbp-server-mh2/301.lsusb (confirms `0451:16a0` and `0403:6001` are real connected devices,
    not corpus typos — used as documentation only);
    .planning/phases/09-round-trip-verification/09-REPLAN-NOTES.md (explains why this test no
    longer needs a `depends_on: 09-02` or a precondition gating on an in-phase USB fix — Phase 8.1
    already shipped it, more thoroughly, before this plan runs).
  </read_first>
  <behavior>
    - Test: `zbp_server_mh2_301_full_round_trip_produces_valid_qemu_cmdline` — the full four-stage
      chain completes with zero panics/errors, INCLUDING usb4/usb5's vendor:product-ID passthrough
      entries (this exercises Phase 8.1's typed `UsbHostIdentity::VendorProduct` path against real
      corpus data as a regression guard, not just Phase 8.1's own unit tests) and all 4
      `virtio-scsi-single` per-disk controllers.
    - Test: cmdline contains exactly 6 `usb-host` device entries (usb0 through usb5).
    - Test: of those, exactly 4 contain `hostbus=`/`hostport=` (usb0-usb3, bus-port form) and exactly
      2 contain `vendorid=0x0451`/`productid=0x16a0` (usb4) and `vendorid=0x0403`/`productid=0x6001`
      (usb5) respectively.
    - Test: cmdline contains exactly 1 `vfio-pci` entry (hostpci0, 1 function, no x-vga).
    - Test: cmdline contains a `virtio-net-pci` device with `netdev=net0` (net0 present, unlike
      coruscant).
    - Test: cmdline contains exactly 4 occurrences of a `virtio-scsi-pci,id=scsihw{N}` controller
      line for N in 0..=3 (each `scsiN` disk gets its own `virtio-scsi-single`-flavored controller
      instance, emitted using the `virtio-scsi-pci` device model per Phase 8.1's
      `emit_virtio_scsi_single`, per `pcie.rs`) — this is the controller-type-specific assertion
      Phase 8.1 made possible, replacing the old generic-presence-only approach.
  </behavior>
  <action>
    Add `#[test] fn zbp_server_mh2_301_full_round_trip_produces_valid_qemu_cmdline()` to
    `tests/round_trip_verification.rs`, mirroring Task 1's coruscant test structure:
    `load_corpus("zbp-server-mh2", "301.conf")` → `ProxmoxImporter::new(vm_conf, storage_conf, 301).into_runtime().expect(...)`
    (note: this corpus's active section has no `args:` key, so skip the RawArgs-verbatim assertion
    for this test — do not `.expect()` a `vm_conf.args` that doesn't exist) → YAML round-trip
    (identical pattern to Task 1) → `runtime_for_cmdline(...)` → `QemuCommandLine::try_from((runtime,
    make_ctx("zbp-server-mh2"))).expect(...)` → `output = cmdline.to_string()`. Assert:
    `output.matches("usb-host").count() == 6`; `output.matches("hostbus=").count() == 4`;
    `output.contains("vendorid=0x0451")` and `output.contains("productid=0x16a0")`;
    `output.contains("vendorid=0x0403")` and `output.contains("productid=0x6001")`;
    `output.matches("vfio-pci").count() == 1`; `output.contains("virtio-net-pci")` and
    `output.contains("netdev=net0")`; exactly 4 occurrences matching `id=scsihw{N}` for N in 0..=3
    (e.g. `output.contains("id=scsihw0")`, `output.contains("id=scsihw1")`,
    `output.contains("id=scsihw2")`, `output.contains("id=scsihw3")`, plus
    `output.matches("virtio-scsi-pci").count() == 4` to confirm each is emitted with the
    `virtio-scsi-pci` device model per `emit_virtio_scsi_single`) with each controller's `-object
    iothread{N}` preceding its `-device` line (read `emit_virtio_scsi_single` in `pcie.rs` first to
    confirm the exact ordering/id format before writing the assertion, per the `<read_first>` note —
    id formats differ between index 0 and indices 1-3, do not assume uniformity).
  </action>
  <verify>
    <automated>cargo test --test round_trip_verification zbp -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test round_trip_verification zbp -- --nocapture` exits 0 with
      `zbp_server_mh2_301_full_round_trip_produces_valid_qemu_cmdline` reported as passed, with zero
      panic output.
    - `cargo test` (full suite, both plans' tests combined) exits 0 — final count is STATE.md's
      165 pre-phase (post-Phase-8.1) tests plus this phase's net-new tests (1 felucia + 1 coruscant +
      1 zbp = 3 net-new `#[test]` functions minimum).
    - `grep -c "id=scsihw0\|id=scsihw1\|id=scsihw2\|id=scsihw3" tests/round_trip_verification.rs`
      returns at least `1` (the per-instance controller-type-specific assertion is present, not
      omitted).
  </acceptance_criteria>
  <done>zbp-server-mh2/301's full four-stage round trip is proven end-to-end, including its 6 USB devices (4 bus-port + 2 vendor:product-ID, both already supported by Phase 8.1's typed UsbHostIdentity), 1 hostpci device, 1 net device, and 4 per-disk virtio-scsi-single controllers (controller-type-specific assertions, not generic) — completing QEMU-04's three-corpus-file coverage with zero errors/panics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus files → parser | `input/coruscant/501.conf`/`storage.cfg` and `input/zbp-server-mh2/301.conf`/`storage.cfg` are trusted, checked-in fixture files — no new input-parsing surface added |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Denial of Service | zbp-server-mh2/301's usb4/usb5 vendor:product-ID emission path (`UsbHostIdentity::VendorProduct` arm in `emit_usb_device`) | low | accept | This path already shipped and was independently verified in Phase 8.1 (7/7 must-haves, no panic path — an exhaustive match on a typed enum, not a fallible string split); this plan's test is a real-corpus regression guard on top of Phase 8.1's own unit tests, not a first-time exercise of unproven code. No cross-plan dependency risk remains since the fix predates this plan's execution. |

</threat_model>

<verification>
`cargo test --test round_trip_verification` (all 3 tests across Plans 09-01/09-03) passes; `cargo test` (full suite) passes with no regressions.
</verification>

<success_criteria>
All three D-03-selected corpus files (felucia/108, coruscant/501, zbp-server-mh2/301) round-trip
end-to-end through all four pipeline stages with zero errors/panics and pass D-04's structural
spot-checks — including controller-type-specific scsihw device string assertions and USB
vendor:product-ID passthrough assertions, both enabled by Phase 8.1's landed schema work —
completing QEMU-04.
</success_criteria>

<output>
Create `.planning/phases/09-round-trip-verification/09-03-SUMMARY.md` when done
</output>
