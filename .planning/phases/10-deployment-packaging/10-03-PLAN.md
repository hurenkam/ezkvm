---
phase: 10-deployment-packaging
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - doc/dev/PACKAGING.md
  - doc/dev/MANUAL-VERIFICATION.md
autonomous: true
requirements: [DEPLOY-02, DEPLOY-03]

must_haves:
  truths:
    - "doc/dev/PACKAGING.md documents the ezkvm-group/FHS access-control model, the qemu-system-x86 package-naming pitfall, why looking-glass-client is Recommends-only, the no-AppArmor/SELinux 'unconfined' v1 stance (D-06), and the Debian revision-suffix convention (D-07)"
    - "doc/dev/MANUAL-VERIFICATION.md explicitly documents felucia/108's real-GPU/USB-passthrough boot as a manual-only, non-CI verification step, and is never referenced by any automated CI check"
  artifacts:
    - "doc/dev/PACKAGING.md"
    - "doc/dev/MANUAL-VERIFICATION.md"
  key_links:
    - "doc/dev/PACKAGING.md cross-references debian/host.yaml.example's inline convention notes (Plan 10-01) so the convention is documented in exactly one authoritative place, not duplicated ad hoc"
---

<objective>
Document this phase's packaging decisions and distro-specific differences (ROADMAP Phase 10
success criterion #5), and produce an explicit, clearly-labeled manual-only verification checklist
for `felucia/108.conf`'s real-hardware (GPU/USB passthrough) boot — separating it unambiguously
from the automated CI matrix Plans 10-01/10-02 built, per RESEARCH.md's Pitfall 5.

Purpose: Close the loop on ROADMAP's fifth success criterion ("distro-specific packaging
differences are documented; no AppArmor/SELinux profile shipped in v1") and make explicit — in a
form a human reviewer can find without reading `10-RESEARCH.md` — that felucia/108's real-hardware
boot is intentionally out of this phase's automated scope, not silently skipped.
Output: Two new docs — `doc/dev/PACKAGING.md` (packaging architecture/decisions reference) and
`doc/dev/MANUAL-VERIFICATION.md` (felucia/108 real-hardware checklist, explicitly non-automated).
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-deployment-packaging/10-CONTEXT.md
@.planning/phases/10-deployment-packaging/10-RESEARCH.md
@input/felucia/108.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: doc/dev/PACKAGING.md — packaging architecture and distro-difference reference</name>
  <files>doc/dev/PACKAGING.md</files>
  <read_first>
    `10-CONTEXT.md`'s D-01 through D-08 (all locked decisions this doc must reflect accurately);
    `10-RESEARCH.md`'s Package Legitimacy Audit table (per-package availability across the 4
    targets) and Pitfall 1/4 (package-naming and `Recommends`-only rationale); Plan 10-01's
    `debian/host.yaml.example` header comment (the revision-suffix convention text already
    written there — this doc should reference it, not duplicate a possibly-drifting copy).
  </read_first>
  <action>
    Write `doc/dev/PACKAGING.md` covering: (1) the single musl-static `.deb` architecture and why
    (D-08 — one package across Debian 13/12, Ubuntu 26.04/24.04, eliminating glibc
    symbol-versioning fragility); (2) the FHS layout (`/etc/ezkvm`, `/var/lib/ezkvm`,
    `/run/ezkvm` via tmpfiles.d — D-04) and the `ezkvm` group access-control model (D-03 — no
    libvirt dependency, users must be added to the group, document what that grant means
    security-wise, matching the language already in `debian/host.yaml.example`); (3) the
    `Depends`/`Recommends` split and why (`qemu-system-x86` is the correct Debian/Ubuntu binary
    package name — NOT `qemu-system-x86_64` — hard `Depends`; `looking-glass-client` is
    `Recommends`-only because it's absent from 3 of the 4 targets' apt archives, with a pointer
    to installing it from upstream `looking-glass.io` release tarballs on those targets); (4) an
    explicit statement that no AppArmor/SELinux profile ships in v1 — the package runs
    "unconfined," profile authoring is deferred (D-06); (5) the Debian revision-suffix convention
    (cargo-deb's default `-1` tracks `Cargo.toml`'s version per D-07; bump to `-2`/`-3` for
    packaging-only re-releases); (6) a short table of any distro-specific quirks actually
    encountered while building/running Plans 10-01/10-02's CI matrix (leave this table with a
    single placeholder row — "None encountered as of initial packaging (2026-07-30); update this
    table if Plan 10-02's 4-target matrix surfaces a real per-target difference" — do not invent
    fictitious quirks that haven't actually been observed).
  </action>
  <verify>
    <automated>test -f doc/dev/PACKAGING.md && grep -c "unconfined" doc/dev/PACKAGING.md | grep -qv '^0$' && grep -c "qemu-system-x86" doc/dev/PACKAGING.md | grep -qv '^0$' && grep -c "Recommends" doc/dev/PACKAGING.md | grep -qv '^0$'</automated>
  </verify>
  <done>`doc/dev/PACKAGING.md` exists and documents all 8 locked CONTEXT.md decisions (D-01–D-08) in reviewer-readable form, cross-referencing rather than duplicating `debian/host.yaml.example`'s convention text.</done>
</task>

<task type="auto">
  <name>Task 2: doc/dev/MANUAL-VERIFICATION.md — felucia/108 real-hardware boot checklist (manual-only)</name>
  <files>doc/dev/MANUAL-VERIFICATION.md</files>
  <read_first>
    `10-RESEARCH.md` Pitfall 5 in full (why `felucia/108.conf` cannot boot in any CI environment —
    physical GPU at `0000:03:00` and physical USB device at bus-port `1-2.2` that no container or
    standard CI VM can provide); `input/felucia/108.conf` (confirm the exact `hostpci0`/`usb0`
    lines this checklist should tell a human operator to expect/verify).
  </read_first>
  <action>
    Write `doc/dev/MANUAL-VERIFICATION.md` with a prominent top-of-file banner stating this
    document describes a MANUAL, NOT AUTOMATED verification step that is never run by CI and does
    not gate this phase's (or any phase's) automated completion — it requires physical hardware
    (a discrete GPU at PCI address `0000:03:00` and a USB device at bus-port `1-2.2`) that cannot
    exist in any container or standard CI runner. Below the banner, list the manual steps: install
    the packaged `.deb` on a real Debian 13 or Ubuntu 26.04 host that has the `wakiza`/felucia-108
    hardware physically attached; place `felucia/108.conf`'s already-round-tripped ezkvm YAML
    (from Phase 9's `tests/round_trip_verification.rs` output, or a fresh Proxmox import) at
    `/etc/ezkvm/vm.d/wakiza.yaml`; run `ezkvm start wakiza`; confirm the QEMU process starts, the
    GPU/USB devices attach without a `vfio`/`usb-host` error, and (if a display client is
    configured) Looking Glass or `remote-viewer` successfully connects; run `ezkvm stop wakiza`
    to confirm graceful shutdown. State explicitly that a failure here is a real-hardware/BIOS/
    IOMMU-configuration concern, not a defect in this phase's packaging or CI verification work.
  </action>
  <verify>
    <automated>test -f doc/dev/MANUAL-VERIFICATION.md && grep -ci "manual" doc/dev/MANUAL-VERIFICATION.md | grep -qv '^0$' && grep -c "wakiza\|felucia" doc/dev/MANUAL-VERIFICATION.md | grep -qv '^0$' && ! grep -rl "MANUAL-VERIFICATION" .github/workflows/ 2>/dev/null</automated>
  </verify>
  <done>`doc/dev/MANUAL-VERIFICATION.md` exists, is prominently labeled manual/non-automated, documents felucia/108's real-hardware boot checklist, and is not referenced anywhere in `.github/workflows/` (confirming it never gates automated CI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan produces only Markdown documentation; no executable code, scripts, or install-time privilege boundaries are introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Information Disclosure | `doc/dev/MANUAL-VERIFICATION.md` documents a real host's hardware topology (PCI/USB addresses) | low | accept | These addresses are already public in the checked-in `input/felucia/108.conf` fixture; the doc adds no new disclosure surface |

</threat_model>

<verification>
`doc/dev/PACKAGING.md` and `doc/dev/MANUAL-VERIFICATION.md` both exist, contain the required
keyword coverage (verified via `grep`), and the manual-verification doc is confirmed absent from
any `.github/workflows/` reference (proving it cannot silently gate automated CI).
</verification>

<success_criteria>
ROADMAP Phase 10 success criterion #5 is satisfied: distro-specific packaging differences have a
documented home, the v1 "unconfined" (no AppArmor/SELinux) stance is explicit, and felucia/108's
real-hardware boot is documented as an explicitly manual, non-blocking, non-CI verification step.
</success_criteria>

<output>
Create `.planning/phases/10-deployment-packaging/10-03-SUMMARY.md` when done
</output>
