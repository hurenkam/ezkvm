---
phase: 10-deployment-packaging
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - .github/workflows/package-verify.yml
  - Cargo.toml
autonomous: true
requirements: [DEPLOY-02, DEPLOY-03, DEPLOY-04, QEMU-04]

must_haves:
  truths:
    - "The same musl-static .deb built by Plan 10-01's build-deb job installs cleanly and passes the full start/status/stop/kill/reset lifecycle on all 4 required targets: debian:bookworm, debian:trixie, ubuntu:24.04, ubuntu:26.04 (D-08)"
    - "The built .deb's Installed-Size stays within a sane bound (not accidentally hundreds of MB from an unstripped musl static link — Pitfall 6)"
  artifacts:
    - ".github/workflows/package-verify.yml (matrix expanded from 1 to 4 targets)"
  key_links:
    - "verify job matrix.image -> one container per target -> same install+lifecycle script Plan 10-01 proved on debian:bookworm alone"
---

<objective>
Expand Plan 10-01's tracer CI verification from one target (`debian:bookworm`) to all four targets
D-08 requires: Debian 13 (trixie), Debian 12 (bookworm), Ubuntu 26.04, Ubuntu 24.04 — proving the
single musl-static `.deb`'s cross-distro/cross-version compatibility claim, not just the tracer's
happy-path single target. Also adds a binary-size sanity check (Pitfall 6) so an unstripped or
misconfigured musl build doesn't silently balloon the shipped artifact.

Depends on Plan 10-01 because both plans modify `.github/workflows/package-verify.yml` (the matrix
array Plan 10-01 seeded with one entry) — same-file writes must be sequential, not parallel.

Purpose: Satisfy ROADMAP Phase 10's success criteria #2–#4 (Debian 13/Ubuntu 26.04 "current focus"
plus Debian 12/Ubuntu 24.04 "also required") and DEPLOY-02/03/04/QEMU-04 across the full matrix,
not just the tracer's single proof-of-concept target.
Output: `.github/workflows/package-verify.yml`'s `verify` job matrix covers all 4 targets;
`build-deb` job asserts a sane `.deb` size.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-deployment-packaging/10-CONTEXT.md
@.planning/phases/10-deployment-packaging/10-RESEARCH.md
@.github/workflows/package-verify.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CI verify matrix to all 4 target distro/version combinations (D-08)</name>
  <files>.github/workflows/package-verify.yml</files>
  <read_first>
    `.github/workflows/package-verify.yml` as Plan 10-01 left it (single-entry
    `matrix.image: ["debian:bookworm"]`); `10-RESEARCH.md`'s Standard Stack section confirming
    `qemu-system-x86`/`swtpm`/`virt-viewer` package presence identically across all 4 targets
    (bookworm, trixie, noble, resolute) via live `qa.debian.org`/`packages.ubuntu.com` lookups —
    no per-target `Depends:` divergence is expected, only possible base-image apt-update quirks.
  </read_first>
  <action>
    Change `verify` job's `strategy.matrix.image` array from `["debian:bookworm"]` to
    `["debian:bookworm", "debian:trixie", "ubuntu:24.04", "ubuntu:26.04"]` and add
    `strategy.fail-fast: false` so one target's failure doesn't cancel the other three (each
    target's result should be independently visible). Add `env: { DEBIAN_FRONTEND: noninteractive
    }` to the container-exec step to prevent any interactive apt prompts from hanging a
    Ubuntu-base-image job (Debian slim images already default non-interactive; Ubuntu's official
    images sometimes don't). Keep the install+lifecycle script identical across all 4 targets —
    Plan 10-01's research confirmed no target-specific package-name divergence exists for this
    phase's dependency set, so no per-target branching logic should be added unless a real
    failure surfaces during execution (do not speculatively special-case a target that hasn't
    actually failed).
  </action>
  <verify>
    <automated>python3 -c "
import yaml
doc = yaml.safe_load(open('.github/workflows/package-verify.yml'))
images = doc['jobs']['verify']['strategy']['matrix']['image']
assert len(images) == 4, images
assert set(images) == {'debian:bookworm', 'debian:trixie', 'ubuntu:24.04', 'ubuntu:26.04'}, images
print('ok')
"</automated>
  </verify>
  <done>`verify` job's matrix covers exactly the 4 required targets (`debian:bookworm`, `debian:trixie`, `ubuntu:24.04`, `ubuntu:26.04`) with `fail-fast: false`, running the identical install+lifecycle script Plan 10-01 proved on one target.</done>
</task>

<task type="auto">
  <name>Task 2: Binary-size sanity check + Debian revision-suffix note (Pitfall 6, Open Question 3)</name>
  <files>.github/workflows/package-verify.yml, Cargo.toml</files>
  <read_first>
    `10-RESEARCH.md` Pitfall 6 ("Package Installed-Size/binary size surprises from static musl
    linking") and Open Question 3 (Debian revision-suffix convention); the `[profile.release]`
    section Plan 10-01 added to `Cargo.toml` (`lto = true`, `strip = true`).
  </read_first>
  <action>
    Add a step to the `build-deb` job, after `cargo deb --target x86_64-unknown-linux-musl`,
    asserting the produced `.deb`'s file size is under a documented sane upper bound (e.g. 200 MiB
    — a generous ceiling that would only trip if `strip`/LTO settings regressed or an unexpected
    dependency bloat crept in; document the exact number chosen and why in an inline workflow
    comment referencing Pitfall 6). Add a one-line comment directly above `[package.metadata.deb]`
    in `Cargo.toml` documenting the Debian revision-suffix convention already written into
    `debian/host.yaml.example` by Plan 10-01 (cargo-deb's default `-1` suffix tracks `Cargo.toml`'s
    version per D-07; bump to `-2`/`-3` for packaging-only re-releases of the same source version)
    so the convention is discoverable from the config file itself, not only the shipped
    documentation.
  </action>
  <verify>
    <automated>grep -c "Pitfall 6\|Installed-Size\|size" .github/workflows/package-verify.yml | grep -qv '^0$' && grep -c "revision" Cargo.toml | grep -qv '^0$'</automated>
  </verify>
  <done>`build-deb` job fails if the built `.deb` exceeds the documented size ceiling; `Cargo.toml` documents the Debian revision-suffix bump convention adjacent to `[package.metadata.deb]`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 4 parallel CI containers, each with `/dev/kvm` passthrough | Same trust boundary as Plan 10-01, now replicated across 4 targets rather than 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Denial of Service | 4-way parallel matrix job resource contention on shared CI runner pool | low | accept | Standard GitHub Actions matrix behavior (each job gets its own runner); no new mitigation needed beyond `fail-fast: false` isolating failures |
| T-10-06 | Tampering | Unbounded `.deb` size regression silently shipping a bloated/misconfigured artifact | low | mitigate | Task 2's explicit Installed-Size sanity check fails the build before an oversized package reaches any target |

</threat_model>

<verification>
`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/package-verify.yml'))"` parses
cleanly with exactly 4 matrix targets and `fail-fast: false`; `build-deb` job includes a
size-sanity-check step; `Cargo.toml` documents the revision-suffix convention.
</verification>

<success_criteria>
The single musl-static `.deb` is verified (build → install → `ezkvm start`/`status`/`stop`/`kill`/
`reset` against real QEMU) across all 4 required distro/version targets, with a binary-size
regression guard in place.
</success_criteria>

<output>
Create `.planning/phases/10-deployment-packaging/10-02-SUMMARY.md` when done
</output>
