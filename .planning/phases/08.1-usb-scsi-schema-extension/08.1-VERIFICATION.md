---
phase: 08.1-usb-scsi-schema-extension
verified: 2026-07-29T16:52:00+02:00
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps: []
deferred: []
---

# Phase 8.1: USB & SCSI Schema Extension Verification Report

**Phase Goal:** Extend the Runtime model and ezkvm YAML schema to natively and losslessly represent USB vendor:product-ID passthrough identity and multiple SCSI controller types (`virtio-scsi-pci`, `virtio-scsi-single`, in addition to `pvscsi`), so real-world corpus configs using these variants (e.g. zbp-server-mh2/301) round-trip correctly without handler-level point-fixes or lossy collapsing to a single default.
**Verified:** 2026-07-29T16:52:00+02:00
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria, RUNT-10/RUNT-11)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Runtime represents USB identity as a variant covering bus-port and vendor:product-ID forms; QEMU emitter emits correct flags for each without panicking | ✓ VERIFIED | `src/runtime/devices/usb_generic.rs` defines `UsbHostIdentity::{BusPort{bus,port}, VendorProduct{vendor_id,product_id}}` with a fallible `parse()` (no `.expect()`/panic path); `src/config/qemu/handlers/usb.rs::emit_usb_device` matches both variants and emits `hostbus=/hostport=` or `vendorid=0x.../productid=0x...` flags; unit tests `test_07_03_usb_host_passthrough_parses_hostbus_hostport` and `test_07_03_usb_host_passthrough_emits_vendor_product` pass |
| 2 | ezkvm YAML round-trips both USB identity variants losslessly | ✓ VERIFIED | `src/config/ezkvm/schema/usb.rs` defines mirrored tagged `UsbHostIdentitySchema`; `tests/yaml_round_trip.rs::usb_bus_port_round_trips_yaml` and `::usb_vendor_product_round_trips_yaml` pass |
| 3 | Proxmox importer maps both `hostusbN: host=<bus>-<port>` and `host=<vendor>:<product>` forms into the new Runtime USB identity variants | ✓ VERIFIED | `src/config/proxmox/importer.rs:140` calls `UsbHostIdentity::parse(&usb_conf.host)?` at the import boundary; importer unit tests `test_...usb0_parsed_into_usb_bus_host_passthrough`, `test_...usb_vendor_product_parsed_into_usb_bus_host_passthrough`, and `test_...usb_malformed_host_identity_returns_typed_error` all pass; real corpus `input/zbp-server-mh2/301.conf` contains both forms (`usb0: host=1-4` ... `usb4: host=0451:16a0`) and is imported successfully by `zbp_server_mh2_301_virtio_scsi_single_round_trips_yaml`'s `.into_runtime().expect(...)` call, which processes the whole conf including these USB lines |
| 4 | Runtime represents SCSI controller type as a first-class field; QEMU emitter emits corresponding controller device type | ✓ VERIFIED | `src/runtime/devices/pvscsi.rs` defines `GenericScsiController { controller_type: ScsiControllerType, scsi_bus }` with `ScsiControllerType::{PvScsi, VirtioScsiPci}`; `src/config/qemu/handlers/pcie.rs::emit_scsi_controller` dispatches `"pvscsi"` vs `"virtio-scsi-pci"` device strings; tests `test_08_01_pvscsi_controller_line_is_unchanged` and `test_08_01_virtio_scsi_pci_controller_emits_correct_device_model` pass |
| 5 | ezkvm YAML round-trips the SCSI controller type field losslessly | ✓ VERIFIED | `src/config/ezkvm/schema/pcie.rs::PcieDeviceTypeSchema::ScsiController{controller_type}` + `ScsiControllerTypeSchema`; `tests/yaml_round_trip.rs::virtio_scsi_pci_controller_type_round_trips_yaml` and `::coruscant_100_virtio_scsi_pci_round_trips_yaml` (real corpus `input/coruscant/100.conf`, `scsihw: virtio-scsi-pci`) pass |
| 6 | Proxmox importer's `scsihw` value is threaded into Runtime's SCSI controller type field instead of parsed-then-discarded | ✓ VERIFIED | `src/config/proxmox/importer.rs:52-54` matches `self.vm_conf.scsihw.as_deref()` into `ScsiControllerType::{VirtioScsiPci, PvScsi}` and passes it into `GenericScsiControllerBuilder::with_controller_type(...)`; importer tests `test_08_01_importer_maps_virtio_scsi_pci_to_controller_type` and `test_08_01_importer_defaults_missing_scsihw_to_pvscsi` pass |
| 7 | zbp-server-mh2/301 corpus (vendor:product-ID USB, `virtio-scsi-single`) is usable as Phase 9 test input without handler-level workaround | ✓ VERIFIED | `src/config/proxmox/importer.rs:41-49`: when `scsihw == "virtio-scsi-single"`, one `VirtioScsiSingleDisk` (a merged Pcie+Scsi+Storage device, no internal `scsi_bus`) is created per `scsiN` entry, keyed on the Proxmox index, at PCIe slot `16+idx`; `zbp_server_mh2_301_virtio_scsi_single_round_trips_yaml` confirms exactly 4 such devices at slots `(16,0),(17,1),(18,2),(19,3)` import correctly and round-trip through YAML byte-for-byte-equivalent; the collision-free QEMU emission is separately proven by `test_08_01_virtio_scsi_single_emits_distinct_ids_for_four_controllers`, which asserts unique `id=iothreadN`, `id=scsihwN`, `id=drive-scsiN-0`/`id=scsi0`, and `bus=scsihwN.0` tokens across 4 instances |

**Score:** 7/7 truths verified (0 present, behavior-unverified)

### Locked Decisions Spot-Check (08.1-CONTEXT.md)

| Decision | Status | Evidence |
|----------|--------|----------|
| D-01 (typed USB identity, parsed once at import boundary) | ✓ VERIFIED | Parsing happens exactly once in `ProxmoxImporter::into_runtime` (`importer.rs:140`); `UsbHostIdentity::parse` returns `Result<_, ProxmoxImportError>` (typed `MalformedUsbHostIdentity{raw}` variant in `src/config/proxmox/error.rs:37`), not a panic; the QEMU emitter (`handlers/usb.rs`) only ever matches an already-typed enum and cannot panic on malformed strings |
| D-02 (GenericScsiController, generalized shared-bus controller) | ✓ VERIFIED | `GenericScsiController{controller_type, scsi_bus: HashMap<ScsiAddress, Arc<dyn ScsiDevice>>}` in `pvscsi.rs`; `pvscsi`/`virtio-scsi-pci` share identical bus-attached-disk structural behavior, differing only in `controller_type` and emitted device string |
| D-03 (VirtioScsiSingleDisk, separate merged device, LOCKED) | ✓ VERIFIED | `VirtioScsiSingleDisk{resource, storage_type, index}` implements `PcieDevice`, `ScsiDevice`, and `StorageDevice` directly (`pvscsi.rs`), with **no** `scsi_bus` field; one instance created per Proxmox `scsiN` entry (importer.rs:41-49); multi-instance collision-freedom proven by `test_08_01_virtio_scsi_single_emits_distinct_ids_for_four_controllers` |
| D-04 (no backward-compat shim/migration) | ✓ VERIFIED | No migration/compat/shim code found in modified schema/builder files (`grep -i "migrat\|compat\|legacy\|shim"` on `schema/pcie.rs`, `schema/usb.rs`, `runtime/builder.rs` returns only one unrelated string constant about legacy PCI *bus numbering*, not a SCSI/USB compat shim); YAML shape for `PcieDeviceTypeSchema::ScsiController`/`VirtioScsiSingle` is new, no back-compat bare-`PvScsi` tag preserved |
| D-06 (scope boundary — no Phase-9-style round-trip integration tests) | ✓ VERIFIED | No `tests/round_trip_verification.rs` exists (that remains Phase 9's file per ROADMAP); this phase's tests live in `tests/yaml_round_trip.rs` (Runtime↔YAML schema round trip only, no QEMU cmdline generation) and `tests/proxmox_import.rs`/inline unit tests (importer/emitter structural checks) — consistent with unit/structural-level scope |
| D-07 (PciBusDeviceKind::PvScsi / IsaBusDeviceKind::PvScsi left as-is, no new wiring) | ✓ VERIFIED | `GenericScsiController`'s `PciDevice`/`IsaDevice` impls still return `PciBusDeviceKind::PvScsi` / `IsaBusDeviceKind::PvScsi` (unchanged names); `src/config/ezkvm/schema/pci.rs`'s `PciDeviceTypeSchema` enum still has only `NetworkController/QxlGpu/Ac97/HostPci` (no new SCSI variant added); no `src/config/ezkvm/schema/isa.rs` or `src/config/qemu/handlers/isa.rs` exists |
| D-08 (virtio-scsi-single instance/label uniqueness keyed on Proxmox scsiN index) | ✓ VERIFIED | `importer.rs:48`: `VirtioScsiSingleDisk::new(resolved, storage_type, *idx)` where `idx` is the Proxmox `scsiN` index; `handlers/pcie.rs::emit_virtio_scsi_single` uses `disk.index()` directly as `iothread{id}`/`scsihw{id}` |
| D-09 (ScsiControllerType introduced at Proxmox-import boundary, not in conf.rs) | ✓ VERIFIED | `conf.rs`'s `scsihw: Option<String>` parsing is untouched; the string→`ScsiControllerType` match happens in `importer.rs:52-54`, mirroring D-01's USB pattern |

### Research Pitfalls Addressed (08.1-RESEARCH.md)

| Pitfall | Status | Evidence |
|---------|--------|----------|
| Pitfall 1 (5-layer USB/SCSI propagation, not just the 4 named in CONTEXT.md) | ✓ VERIFIED | All 5 layers confirmed present and exhaustively matching: Runtime types (`usb_generic.rs`, `pvscsi.rs`), ezkvm schema (`schema/usb.rs`, `schema/pcie.rs`), Proxmox importer (`importer.rs`), QEMU emitter (`handlers/usb.rs`, `handlers/pcie.rs`), **and** the two intermediate conversion layers RESEARCH.md flagged as missing from CONTEXT.md's list: `config/ezkvm/runtime/parser.rs` (Runtime→schema, 3 match arms updated) and `config/ezkvm/runtime/builder.rs` (schema→Runtime, `match_usb_device`/`match_scsi_device` updated), plus `runtime/q35.rs` (`format_usb_device`/display formatting for both USB variants and the generalized SCSI controller). `cargo build` has zero non-exhaustive-match errors, confirming no site was missed |
| Pitfall 2 (`emit_pvscsi`/`emit_scsi_storage` id-collision risk for N `virtio-scsi-single` instances) | ✓ VERIFIED | `emit_virtio_scsi_single` derives `controller_id` from `disk.index()` (not a constant); `storage::emit_scsi_storage` was extended with a `controller_id: u8` parameter that changes drive/device id format when `controller_id != 0`; the dedicated test `test_08_01_virtio_scsi_single_emits_distinct_ids_for_four_controllers` asserts each of 4 emitted instances has exactly one occurrence of each `id=iothreadN`/`id=scsihwN`/drive/device id token — directly proving no collision across N instances |
| Pitfall 3 / D-07 (vestigial `PciBusDeviceKind::PvScsi`/ISA paths must NOT gain new schema/handler wiring) | ✓ VERIFIED | Confirmed above under D-07 — no `PciDeviceTypeSchema` SCSI variant, no ISA schema/handler files added |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/runtime/devices/usb_generic.rs` | Typed `UsbHostIdentity` + parser | ✓ EXISTS + SUBSTANTIVE + WIRED | Enum, fallible parser, 3 unit tests |
| `src/config/qemu/handlers/usb.rs` | Panic-free USB emission for both identity forms | ✓ EXISTS + SUBSTANTIVE + WIRED | No `.expect()`/panic on parsing; dispatches both variants |
| `src/config/ezkvm/schema/usb.rs` | Nested tagged USB identity schema | ✓ EXISTS + SUBSTANTIVE + WIRED | `UsbHostIdentitySchema` mirrors Runtime enum |
| `src/runtime/devices/pvscsi.rs` | `GenericScsiController` + `VirtioScsiSingleDisk` | ✓ EXISTS + SUBSTANTIVE + WIRED | Both types present with correct trait impls |
| `src/config/ezkvm/schema/pcie.rs` | `ScsiController`/`VirtioScsiSingle` schema variants | ✓ EXISTS + SUBSTANTIVE + WIRED | Both variants present with round-trip unit tests |
| `src/config/proxmox/importer.rs` | `scsihw` consumption + per-disk virtio-scsi-single creation | ✓ EXISTS + SUBSTANTIVE + WIRED | Lines 41-63; not merely parsed-and-discarded |
| `src/config/qemu/handlers/pcie.rs` | Controller-type-aware + collision-free SCSI emission | ✓ EXISTS + SUBSTANTIVE + WIRED | `emit_scsi_controller`, `emit_virtio_scsi_single` |
| `tests/yaml_round_trip.rs` | USB + SCSI round-trip coverage incl. real corpus | ✓ EXISTS + SUBSTANTIVE + WIRED | 8 tests, including `coruscant_100_...` and `zbp_server_mh2_301_...` |

**Artifacts:** 8/8 verified

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `ProxmoxImporter::into_runtime` | `UsbHostIdentity::parse` | direct call at usb import loop | ✓ WIRED | `importer.rs:140` |
| `ProxmoxImporter::into_runtime` | `ScsiControllerType`/`VirtioScsiSingleDisk` | `scsihw` match | ✓ WIRED | `importer.rs:41-63` |
| `config/ezkvm/runtime/parser.rs` (Runtime→YAML) | `UsbHostIdentitySchema`/`PcieDeviceTypeSchema::ScsiController` | exhaustive match arms | ✓ WIRED | `parser.rs:405-420` (usb), `ScsiController` arm present |
| `config/ezkvm/runtime/builder.rs` (YAML→Runtime) | `UsbHostIdentity`/`ScsiControllerType` | `match_usb_device`/scsi builder | ✓ WIRED | `builder.rs:317-345` |
| `handlers/pcie.rs::emit_pcie_device` | `emit_scsi_controller`/`emit_virtio_scsi_single` | `device_kind()` dispatch | ✓ WIRED | `pcie.rs:29-42` |
| `handlers/pcie.rs::emit_virtio_scsi_single` | `handlers/storage.rs::emit_scsi_storage` | explicit `controller_id` param | ✓ WIRED | `pcie.rs:120-135`, `storage.rs:22-39` |

**Wiring:** 6/6 connections verified

### Behavioral Spot-Checks / Test Execution (run independently by verifier, not trusted from SUMMARY.md)

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| Full workspace build | `cargo build` (forced rebuild via `touch src/main.rs`) | `Finished` with 0 warnings/errors | ✓ PASS |
| Full test suite | `cargo test` | 165 passed, 0 failed across all binaries (lib 102, cli_security 0*, proxmox_import 7, qemu_cmdline 5, qmp_client 15, runtime_phase2 5, ui_client_mapping 6, vm_lifecycle 12, yaml_round_trip 8) | ✓ PASS |
| zbp-server-mh2/301.conf (virtio-scsi-single + vendor:product USB) imports cleanly | `zbp_server_mh2_301_virtio_scsi_single_round_trips_yaml` (`tests/yaml_round_trip.rs`) | pass — 4 distinct disks at slots 16-19, full `.into_runtime()` succeeds incl. USB vendor:product lines | ✓ PASS |
| coruscant/100.conf (virtio-scsi-pci) imports and round-trips | `coruscant_100_virtio_scsi_pci_round_trips_yaml` (`tests/yaml_round_trip.rs`) | pass | ✓ PASS |
| No collisions across N virtio-scsi-single instances | `test_08_01_virtio_scsi_single_emits_distinct_ids_for_four_controllers` (`handlers/pcie.rs`) | pass | ✓ PASS |

\* `cli_security.rs`, and 3 other binaries, reported "0 passed" in the full-suite run — these are compiled test binaries with 0 test functions relevant to this phase's changes (pre-existing, unrelated to Phase 8.1); not a regression.

*Note: this test-run count (165) matches what 08.1-03-SUMMARY.md claimed — independently reproduced by the verifier, not merely accepted.*

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| RUNT-10 | USB passthrough identity as bus-port or vendor:product-ID, round-tripping losslessly | ✓ SATISFIED | See Truths 1-3 above |
| RUNT-11 | SCSI controller type as first-class field, round-tripping losslessly (no collapse to `pvscsi`) | ✓ SATISFIED | See Truths 4-6 above |

**Coverage:** 2/2 requirements satisfied.

**Note (informational, not a code gap):** `.planning/REQUIREMENTS.md` lines 19-20 and 108-109 still show RUNT-10/RUNT-11 as `[ ]`/"Pending" — this is a bookkeeping artifact of the requirements tracker not yet being updated post-execution, not a code deficiency. Per verification-agent scope this file was not edited; recommend marking both requirements complete now that this report confirms them satisfied.

### Anti-Patterns Found

None. Scanned all 16 modified `src/` files for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER`, "not implemented"/"coming soon" phrasing, empty stub returns, and hardcoded-empty props — zero matches.

### Git State Check

- `git log --oneline -5` shows the most recent commit is `702bdc62 planning phase 8.1`, predating any of this phase's execution — **no commits were made by the 3 execution waves**, consistent with the user's standing no-auto-commit policy and each SUMMARY.md's explicit "no git add/commit/push" note.
- `git status --short` shows 18 modified tracked files plus 3 new untracked `*-SUMMARY.md` files — all uncommitted, matching the plan/summary claims. No unexpected commits from any other actor were found in the recent log.

### Human Verification Required

None. All 7 ROADMAP success criteria and all 9 locked decisions (D-01–D-09) are structurally verifiable via source inspection and automated tests, and were independently re-derived by the verifier (not accepted from SUMMARY.md text alone).

### Gaps Summary

No gaps found. All Roadmap success criteria, requirement wording (RUNT-10/RUNT-11), locked context decisions, and research-identified pitfalls are reflected in the current source tree, and the full test suite (165 tests) passes on a from-scratch `cargo build`.

---

_Verified: 2026-07-29T16:52:00+02:00_
_Verifier: the agent (gsd-verifier)_
