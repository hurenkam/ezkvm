---
phase: 08.1-usb-scsi-schema-extension
plan: 02
subsystem: runtime
tags: [rust, scsi, pvscsi, virtio-scsi-pci, proxmox, qemu, yaml]
requires:
  - phase: 08.1-01
    provides: USB identity typing and prior shared conversion-layer baseline
provides:
  - Typed shared SCSI controllers in Runtime via GenericScsiController and ScsiControllerType
  - Proxmox scsihw import preserving pvscsi vs virtio-scsi-pci semantics
  - ezkvm YAML schema/runtime round-trip preservation of controller_type
  - QEMU PCIe emission dispatching pvscsi vs virtio-scsi-pci device models
affects: [phase-09-round-trip-verification, runtime, proxmox-import, ezkvm-schema, qemu-emission]
tech-stack:
  added: []
  patterns: [typed shared SCSI controller parameterized by controller_type, schema-to-runtime enum mapping for bus-grouped devices]
key-files:
  created: [.planning/phases/08.1-usb-scsi-schema-extension/08.1-02-SUMMARY.md]
  modified:
    - src/runtime/devices/pvscsi.rs
    - src/runtime/pcie.rs
    - src/runtime/q35.rs
    - src/runtime.rs
    - src/config/ezkvm/schema/pcie.rs
    - src/config/ezkvm/runtime/parser.rs
    - src/config/ezkvm/runtime/builder.rs
    - src/config/proxmox/importer.rs
    - src/config/qemu/handlers/pcie.rs
    - src/config/qemu/handlers/pci.rs
    - src/config/qemu/handlers/root.rs
    - tests/yaml_round_trip.rs
    - tests/proxmox_import.rs
key-decisions:
  - "Renamed only the PCIe bus-device-kind path to ScsiController; PCI/ISA PvScsi variants stayed unchanged per phase scope."
  - "Kept virtio-scsi-single as an importer-side placeholder mapped to VirtioScsiPci, deferring real per-disk modeling to Plan 08.1-03."
  - "Renamed the emitter helper to emit_scsi_controller to reflect the generalized shared-controller behavior."
patterns-established:
  - "Runtime shared-bus controller types can preserve model identity via a small enum without changing attached-device structure."
  - "Bus-grouped schema builders can carry controller metadata alongside collected child devices via a spec struct."
requirements-completed: [RUNT-11]
coverage:
  - id: D1
    description: Shared SCSI controllers import and emit with preserved pvscsi vs virtio-scsi-pci type information.
    requirement: RUNT-11
    verification:
      - kind: unit
        ref: cargo test --lib scsi
        status: pass
      - kind: unit
        ref: src/config/proxmox/importer.rs#test_08_01_importer_maps_virtio_scsi_pci_to_controller_type
        status: pass
    human_judgment: false
  - id: D2
    description: controller_type survives Runtime -> ezkvm YAML -> Runtime round trips, including coruscant/100.conf.
    requirement: RUNT-11
    verification:
      - kind: integration
        ref: cargo test --test yaml_round_trip
        status: pass
      - kind: unit
        ref: tests/yaml_round_trip.rs#coruscant_100_virtio_scsi_pci_round_trips_yaml
        status: pass
    human_judgment: false
duration: 35min
completed: 2026-07-29
status: complete
---

# Phase 08.1 Plan 02 Summary

**Shared SCSI controllers now preserve and emit pvscsi vs virtio-scsi-pci identity across Proxmox import, ezkvm YAML round-trip, and QEMU output**

## Performance

- **Duration:** 35 min
- **Started:** 2026-07-29T16:35:18+02:00
- **Completed:** 2026-07-29T17:10:18+02:00
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments
- Replaced the hardcoded Runtime `PvScsi` type with `GenericScsiController { controller_type, scsi_bus }` plus `ScsiControllerType::{PvScsi, VirtioScsiPci}`.
- Wired `scsihw` through Proxmox import, PCIe schema/runtime conversion, YAML serialization, and QEMU emission so `virtio-scsi-pci` no longer collapses to `pvscsi`.
- Added regression coverage for importer dispatch, controller-specific QEMU emission, felucia `pvscsi` round-trip stability, and coruscant/100 `virtio-scsi-pci` round-trip preservation.

## Task Commits

Not performed. The user explicitly overrode normal GSD commit behavior and prohibited any `git add`, `git commit`, `git push`, or other history/index-modifying git commands.

## Files Created/Modified
- `src/runtime/devices/pvscsi.rs` - Generalized the shared SCSI controller type and added typed controller variants.
- `src/runtime/pcie.rs` - Renamed the active PCIe bus-device-kind variant to `ScsiController`.
- `src/runtime/q35.rs` - Updated display formatting to downcast/render generalized SCSI controllers.
- `src/runtime.rs` - Re-exported the renamed controller types.
- `src/config/proxmox/importer.rs` - Mapped `scsihw` into `ScsiControllerType` and built shared controllers accordingly.
- `src/config/ezkvm/schema/pcie.rs` - Replaced bare `PvScsi` schema variant with `ScsiController { controller_type }`.
- `src/config/ezkvm/runtime/parser.rs` - Emitted controller_type into schema during Runtime -> YAML conversion.
- `src/config/ezkvm/runtime/builder.rs` - Carried controller_type through grouped SCSI controller specs back into Runtime.
- `src/config/qemu/handlers/pcie.rs` - Dispatched shared SCSI controller emission to `pvscsi` vs `virtio-scsi-pci` device models.
- `src/config/qemu/handlers/pci.rs` - Reused the generalized shared-controller emitter for legacy PCI dispatch.
- `src/config/qemu/handlers/root.rs` - Updated test fixtures to the renamed controller builder.
- `tests/yaml_round_trip.rs` - Added `virtio-scsi-pci` controller-type round-trip coverage, including `coruscant/100.conf`.
- `tests/proxmox_import.rs` - Updated importer assertions to the generalized SCSI controller type.

## Decisions Made
- Preserved `PciBusDeviceKind::PvScsi` and `IsaBusDeviceKind::PvScsi` exactly as-is, while renaming only the active PCIe path to `ScsiController`.
- Used `emit_scsi_controller` as the generalized helper name instead of keeping `emit_pvscsi`.
- Kept unknown or missing `scsihw` values defaulting to `PvScsi`, and kept `virtio-scsi-single` mapped to `VirtioScsiPci` as the explicit interim placeholder required by this plan.

## Deviations from Plan

### Auto-fixed / allowed execution deviations

**1. Emitter helper rename**
- **Issue:** The original helper name `emit_pvscsi` no longer matched the generalized shared-controller responsibility.
- **Fix:** Renamed it to `emit_scsi_controller` and updated PCI/PCIe callers.
- **Impact:** None; explicitly allowed by the plan.

**2. No git commits performed**
- **Issue:** Standard GSD execute-plan workflow expects task/summary commits.
- **Fix:** Skipped all git-modifying commands because the user explicitly forbade them.
- **Impact:** Source and summary changes are complete and verified, but intentionally uncommitted.

**Total deviations:** 2
**Impact on plan:** No functional scope change; all requested behavior and verification completed successfully.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Verification
- `cargo test --lib scsi` ✅ — 15 passed, 0 failed, 83 filtered out.
- `cargo build 2>&1 | tail -40 && cargo test --lib && cargo test --test yaml_round_trip` ✅
  - `cargo build` passed
  - `cargo test --lib` passed: 98 passed, 0 failed
  - `cargo test --test yaml_round_trip` passed: 7 passed, 0 failed
- Final regression rerun requested by user: `cargo build && cargo test --lib && cargo test --test yaml_round_trip` ✅
  - `cargo build` passed
  - `cargo test --lib` passed: 98 passed, 0 failed
  - `cargo test --test yaml_round_trip` passed: 7 passed, 0 failed

## Next Phase Readiness
- Ready for Plan 08.1-03 to replace the temporary `virtio-scsi-single -> VirtioScsiPci` importer mapping with real per-disk merged-device modeling.
- Phase 09 can now rely on schema-level preservation of shared SCSI controller type instead of handler-level workarounds.

---
*Phase: 08.1-usb-scsi-schema-extension*
*Completed: 2026-07-29*
